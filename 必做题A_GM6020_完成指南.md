# 必做题 A 完成指南（GM6020）

> 适用对象：STM32F407 + GM6020（CAN 无刷电机，自带驱动与编码器）
> 现状：现有工程已经是一套 **GM6020 的 CAN 框架**（CAN 收发 + PID 类 + FreeRTOS），方向正确、分层清晰，可以直接复用。
> 目标：在现有框架上补齐「**速度环闭环 + vofa+ 收发协议 + 保护/速度模式切换 + PID 调参**」。

---

## 0. 结论速览

| 项目 | 状态 |
|---|---|
| CAN 收发（GM6020 协议） | ✅ 已有，但**波特率错了**（见 Bug 1） |
| PID 框架 | ✅ 已有类，但 `SingleLoop_PID` / `PID_Clear` **只有声明没实现** |
| 速度环闭环 | ❌ 现在是「位置+速度串级、目标是角度」，要改成**单速度环** |
| vofa+ 通信 | ❌ 只发字符串 `"RM2027"`，没有 JustFloat 帧，也没有接收 |
| 保护模式 / 速度模式切换 | ❌ 未实现 |
| PID 调参 | ❌ Kp/Ki/Kd 全为 0 |

---

## 1. 任务要求回顾

| 模式 | 要求 |
|---|---|
| **保护模式** | 电机不输出任何扭矩（转动输出轴无额外力矩） |
| **速度模式** | 在 vofa+ 上位机中控制电机输出轴以指定角速度旋转，打印「当前转速」和「目标转速」波形 |

验收点（自查明细）：PWM/CAN 控制旋转、编码器读角度和转速、vofa+ 通信、PID 转速闭环、两模式切换、PID 收敛良好、代码分层有注释。

> 注：GM6020 是 CAN 电机，所以「PWM 控制」和「编码器读取」都通过 **CAN 帧**完成：下发电电压 → 电机转动；电机回传编码器角度/转速 → 单片机解析。没有外接 PWM/编码器硬件。

---

## 2. 现有代码盘点

| 文件 | 现在做什么 | 复用结论 |
|---|---|---|
| `Core/Src/can.c` | CAN1 初始化（**波特率错**） | 改 Prescaler 后直接用 |
| `User/Framework/CAN/can_io.cpp` | CAN 发送（0x1FF 下发电压）+ 接收（0x206 解析角度/转速/电流/温度） | ✅ 直接复用，修回调里的局部对象 Bug |
| `User/Framework/PID/pid.cpp` | 位置式 PID + 串级 `Pos_Spd_PID` | 复用，**补** `SingleLoop_PID`、`PID_Clear` |
| `User/Framework/Motor/Motor.cpp` | 调 CAN + 串级 PID 算角度输出 | 改成**速度环** |
| `User/Task/Gimbal_task.cpp` | 500Hz 调 `motor_loop(目标角度)` | 改成调 `motor_loop()`（速度模式） |
| `User/Task/Print_task.cpp` | 50Hz 发 `"RM2027"` | 改成发 **vofa+ JustFloat 帧** |
| `Core/Src/freertos.c` | 三个任务：default / gimbal / print | ✅ 直接复用 |

---

## 3. 未完善部分清单（核心）

1. **【Bug】CAN 波特率错误**：`can.c:41` `Prescaler=3` → 实际 750kbps，GM6020 要 1Mbps。
2. **【缺失】`SingleLoop_PID` 与 `PID_Clear`**：`pid.hpp:51/53` 声明了但 `pid.cpp` 没实现，一旦调用会链接报错。
3. **【改造】速度环**：`Motor.cpp` 现在用 `Pos_Spd_PID`（位置+速度串级，目标角度），要改成单速度环（目标转速）。
4. **【缺失】vofa+ 发送**：`Print_task.cpp` 只发字符串，缺 JustFloat 二进制帧（`00 00 80 7F` 帧尾）。
5. **【缺失】vofa+ 接收**：没有 UART 接收，无法从上位机下目标转速/模式。
6. **【缺失】模式切换**：没有保护模式与速度模式的切换逻辑。
7. **【未做】PID 调参**：`Motor.cpp:21-22` 参数全 0，PID 不起作用。
8. **【Bug】中断回调局部对象**：`can_io.cpp:83-87` 每次中断 `CANc can;` 新建临时对象。
9. **【建议】PID 结构体重复**：`pid.hpp` 里有成员 `SpdParam/PosParam`，`Motor.cpp` 里又定义了一对文件级全局 `Spdparam/Posparam`，应统一成一套。

---

## 4. 分步骤实施

### Step 0：修 CAN 波特率（必须先做，否则通信不通）

STM32F4 CAN 分频公式：

```
实际波特率 = APB1时钟 / (Prescaler + 1) / (1 + BS1 + BS2)
```

本板 APB1 = 42MHz（`.ioc` 里 `APB1Freq_Value=42000000`），当前 BS1=9、BS2=4，即 1+9+4 = 14 个时间量子（TQ）：

- 当前 `Prescaler=3`：`42 / (3+1) / 14 = 0.75 MHz = 750kbps` ❌
- 改成 `Prescaler=2`：`42 / (2+1) / 14 = 1.0 MHz = 1 Mbps` ✅

> ⚠️ 为什么 CubeMX 显示 `CalculateBaudRate=1000000`？因为 CubeMX 用 `÷Prescaler` 计算，而硬件实际是 `÷(Prescaler+1)`。**以公式为准，`Prescaler` 改 2。**

改法（二选一）：
- **CubeMX 里**：打开 `RM2027.ioc` → CAN1 → Parameter Settings → Prescaler 改成 2 → 重新生成代码；或
- **直接改代码**：`Core/Src/can.c:41` 把 `hcan1.Init.Prescaler = 3;` 改成 `= 2;`。

---

### Step 1：补全 PID 的单环与清零函数

`pid.hpp` 里 `SingleLoop_PID` 和 `PID_Clear` 只有声明，把 `PID_Clear` 签名改成带参数（要指定清哪个环），并在 `pid.cpp` 实现：

```cpp
// pid.cpp 追加
float PIDc::SingleLoop_PID(PID_t *WhichPID, float Input, float Target)
{
    PID_Update(WhichPID, Input, Target);
    return PID_Calculate(WhichPID);
}

void PIDc::PID_Clear(PID_t *WhichPID)
{
    WhichPID->PID_Err_now  = 0.0f;
    WhichPID->PID_Err_last = 0.0f;
    WhichPID->PID_Err_all  = 0.0f;
    WhichPID->PID_Out      = 0.0f;
    WhichPID->PID_last_Out = 0.0f;
}
```

`pid.hpp` 对应改：

```cpp
float SingleLoop_PID(PID_t *Which_PID, float Pos_Input, float Pos_Target);
void  PID_Clear(PID_t *WhichPID);   // 原来是 void PID_Clear();
```

---

### Step 2：把 Motor 改成「速度环」

`Motor.hpp` 增加全局控制状态和模式宏，`motor_loop` 去掉角度参数：

```cpp
// Motor.hpp
#define MODE_PROTECT  0   // 保护模式：不输出扭矩
#define MODE_SPEED    1   // 速度模式：速度闭环

extern MOTORc motor;
extern float   target_speed;   // 目标转速 (RPM)，vofa+/按键写入
extern uint8_t motor_mode;     // 当前模式
```

`Motor.cpp` 重写：

```cpp
// Motor.cpp
#include "Motor.hpp"
#include "can_io.hpp"
#include "pid.hpp"

extern Motor_Data Motor_Data_1;

MOTORc motor;
CANc    can;
PIDc    pid;

float   target_speed = 0.0f;
uint8_t motor_mode   = MODE_PROTECT;   // 上电默认保护模式（安全）

#define SPEED_KP  0.0f   // Step 6 再调
#define SPEED_KI  0.0f
#define SPEED_KD  0.0f

void MOTORc::motor_Init()
{
    can.can_init();
    pid.PID_Init(&Spdparam, SPEED_KP, SPEED_KI, SPEED_KD);
}

void MOTORc::motor_loop()
{
    int16_t output = 0;

    if (motor_mode == MODE_SPEED)
    {
        // 单速度环：目标转速 target_speed，反馈 Motor_Data_1.Speed（单位 RPM）
        output = (int16_t)pid.SingleLoop_PID(&Spdparam,
                                             (float)Motor_Data_1.Speed,
                                             target_speed);
    }
    else
    {
        pid.PID_Clear(&Spdparam);   // 清积分，防止切回速度模式瞬间突变
        output = 0;                 // 保护模式：输出 0 电压 = 无扭矩
    }

    // 0x1FF 控制 ID1~4 四个电机；本电机 ID=2，所以电压放第 2 通道(Mess_2)
    can.can_send(0x1FF, 0, output, 0, 0);
}
```

> 说明：`pid.Spdparam` 用的是类成员（`pid.hpp` 里已有 `PID_t SpdParam;`），建议把 `Motor.cpp` 原来的文件级 `PID_t Posparam, Spdparam;` 删掉，统一用成员，避免两套重复。

---

### Step 3：Gimbal_task 改为调速度环

`User/Task/Gimbal_task.cpp` 去掉角度目标：

```cpp
// Gimbal_task.cpp
extern "C" void Gimbal_task(void const * argument)
{
    (void)argument;
    TickType_t current_tick = xTaskGetTickCount();
    const TickType_t cycle_tick = pdMS_TO_TICKS(2);   // 2ms = 500Hz

    for (;;)
    {
        motor.motor_loop();                 // 速度环 + CAN 下发
        vTaskDelayUntil(&current_tick, cycle_tick);
    }
}
```

---

### Step 4：vofa+ 发送（JustFloat 帧）

`User/Task/Print_task.cpp` 改成发送两个 float 通道（目标转速、当前转速）+ 帧尾：

```cpp
// Print_task.cpp
#include "cmsis_os.h"
#include "usart.h"
#include "can_io.hpp"     // Motor_Data
#include <string.h>

extern float   target_speed;
extern Motor_Data Motor_Data_1;

// vofa+ JustFloat：N 个 float32(小端) + 帧尾 00 00 80 7F
static void vofa_send_2float(float ch0, float ch1)
{
    uint8_t buf[12];
    memcpy(buf + 0, &ch0, 4);
    memcpy(buf + 4, &ch1, 4);
    buf[8]  = 0x00;
    buf[9]  = 0x00;
    buf[10] = 0x80;
    buf[11] = 0x7F;
    HAL_UART_Transmit(&huart1, buf, sizeof(buf), HAL_MAX_DELAY);
}

extern "C" void Print_task(void const * argument)
{
    (void)argument;
    TickType_t current_tick = xTaskGetTickCount();
    const TickType_t xPeriod = pdMS_TO_TICKS(10);   // 10ms = 100Hz，波形更平滑

    for (;;)
    {
        vofa_send_2float(target_speed, (float)Motor_Data_1.Speed);
        vTaskDelayUntil(&current_tick, xPeriod);
    }
}
```

vofa+ 端配置：协议选 **JustFloat**，波特率 115200，通道数 **2**。CH0=目标转速，CH1=当前转速。

---

### Step 5：vofa+ 接收（UART 中断 + 简单命令协议）

用 UART 逐字节中断接收，攒成一行命令再解析。协议约定：

- `s500` → 目标转速设为 500 RPM
- `m0`  → 进入保护模式
- `m1`  → 进入速度模式

新建 `User/Framework/Vofa/vofa.cpp`（记得在 `CMakeLists.txt` 加 `include_directories(User/Framework/Vofa)`，`file(GLOB ...)` 已会自动把 `.cpp` 编进去）：

```cpp
// vofa.cpp
#include "usart.h"
#include "Motor.hpp"    // target_speed / motor_mode / MODE_*
#include <stdlib.h>

static uint8_t  rx_line[32];
static uint8_t  rx_idx = 0;
static volatile uint8_t rx_line_ready = 0;
static uint8_t  rx_byte;

void Vofa_RxStart(void)
{
    HAL_UART_Receive_IT(&huart1, &rx_byte, 1);
}

void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    if (huart->Instance != USART1) return;

    if (rx_byte == '\n' || rx_byte == '\r')
    {
        rx_line[rx_idx] = '\0';
        rx_line_ready = 1;
        rx_idx = 0;
    }
    else if (rx_idx < sizeof(rx_line) - 1)
    {
        rx_line[rx_idx++] = rx_byte;
    }
    HAL_UART_Receive_IT(&huart1, &rx_byte, 1);   // 继续收下一字节
}

void Vofa_ParseCommand(void)
{
    if (!rx_line_ready) return;
    rx_line_ready = 0;

    switch (rx_line[0])
    {
        case 's': case 'S':
            target_speed = (float)atof((char *)&rx_line[1]);
            break;
        case 'm':
            if (rx_line[1] == '0') motor_mode = MODE_PROTECT;
            if (rx_line[1] == '1') motor_mode = MODE_SPEED;
            break;
        default:
            break;
    }
}
```

在 `main.cpp` 里 UART 初始化完成后启动接收、并在某个任务里周期解析：

```cpp
// main.cpp
#include "can_io.hpp"
#include "motor.hpp"

void Vofa_RxStart(void);
void Vofa_ParseCommand(void);

int main()
{
    main_init();          // HAL + 时钟 + GPIO + CAN + USART
    motor.motor_Init();   // CAN filter/start + PID 初始化
    Vofa_RxStart();       // 开始 UART 接收
    Rtos_Init();          // 启动调度器
    while (1) {}
}
```

解析可以放在 `Print_task` 循环里（每 10ms 一次足够），在 `vofa_send_2float` 前加一句 `Vofa_ParseCommand();`。

> 进阶（可选）：用 **DMA + UART 空闲中断** 一次性收整段数据，比逐字节中断更省 CPU。但需要 CubeMX 给 USART1 加 DMA_RX 并重新生成，学习阶段逐字节方案已够用。

---

### Step 6：PID 调参（速度环）

GM6020 转速反馈是 **RPM**，输出电压范围 **-30000 ~ +30000**（`pid.hpp` 里 `PID_DEFAULT_OUTPUT_MAX=30000` 正好对上）。

调参顺序（建议）：

1. **先只调 Kp**：Ki=0、Kd=0。从 `Kp=30` 起步，上位机给个阶跃（如 s200），看波形：
   - 收敛慢 → 增大 Kp；
   - 超调/震荡 → 减小 Kp；
   - 直到「快速跟踪、略有一点超调」。
2. **再加 Ki 消静差**：从 `Ki=1` 起，若出现低频震荡或积分饱和就减小；`PID_ErrAllMax` 可适当调小（如 5000）限制积分。
3. **Kd 一般速度环可留 0**：若需要抑制超调，给个很小的值（如 0.5~2）。
4. 记录一组「阶跃响应」vofa+ 截图（目标 vs 当前转速），体现收敛。

> 起始值是估计值，务必上机调。目标：阶跃后转速能较快、无明显震荡地跟上目标值。

---

### Step 7（可选）：物理按键切模式

任务允许「按键 **或** vofa+」切换，vofa+ 的 `m0/m1` 已满足要求。若想加按键加分：

1. CubeMX 配一个 GPIO 输入（上拉）+ 使能该引脚外部中断（EXTI）。
2. 在 `HAL_GPIO_EXTI_Callback` 里翻转 `motor_mode`（带消抖）。

```cpp
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
    if (GPIO_Pin == BTN_PIN)
    {
        motor_mode = (motor_mode == MODE_SPEED) ? MODE_PROTECT : MODE_SPEED;
    }
}
```

---

## 5. 验收清单（提交前对照）

- [ ] CAN 波特率 1Mbps，能收到电机反馈（`Motor_Data_1.Speed` 有值）
- [ ] 保护模式：转动输出轴无阻力（输出 0 电压）
- [ ] 速度模式：vofa+ 下发目标转速，电机按目标转
- [ ] vofa+ 波形：目标转速 + 当前转速两条曲线，能收敛
- [ ] 模式能来回切换（m0/m1 或按键）
- [ ] PID 收敛良好、无明显震荡
- [ ] 代码分层（Framework / Task）、注释清晰
- [ ] 工程名按要求改成 `name_control_exam`
- [ ] 文档附 vofa+ 截图 + Bug/调试记录

---

## 6. 常见 Bug 记录（可直接写进文档）

| # | 现象 | 原因 | 修复 |
|---|---|---|---|
| 1 | 电机完全不响应、收不到反馈 | CAN 波特率 750kbps ≠ 1Mbps | `Prescaler` 3 → 2 |
| 2 | 编译报 `undefined reference to SingleLoop_PID / PID_Clear` | 头文件声明了没实现 | 补 `pid.cpp` 实现 |
| 3 | 切回速度模式瞬间电机猛动 | 积分项残留 | 切模式时 `PID_Clear` |
| 4 | vofa+ 波形乱码/无波形 | 没发 JustFloat 帧尾 `00 00 80 7F`，或协议选错 | 按 Step 4 组帧，vofa+ 选 JustFloat |
| 5 | 中断里新建 `CANc can;` | 每次回调建临时对象（碰巧能用但危险） | 用全局 `can.can_receive()` |
| 6 | 电压发错通道、电机不动 | 电机 ID=2，电压要放第 2 通道 | `can_send(0x1FF, 0, output, 0, 0)` |

---

## 7. 建议实施顺序总结

```
Step 0 修波特率 → Step 1 补 PID 函数 → Step 2/3 速度环 → Step 4 vofa+ 发送
   → Step 5 vofa+ 接收 → Step 6 调参 → Step 7(可选)按键 → 验收/截图
```

每一步改完都能单独编译验证，建议按顺序做、边做边测。
